export type LanguageSchema = {
  common: {
    backHome: string;
    back: string;
  };
  error: {
    notFound: string;
    pageNotFound: string;
    wrong: string;
  };
  app: {
    description: string;
  };
};
