import type { LanguageSchema } from "../type";

const en: LanguageSchema = {
  common: { backHome: "Back Home", back: "Go back" },
  error: {
    notFound: "Not found",
    pageNotFound: "Sorry, the page you visited does not exist!",
    wrong: "Something went wrong"
  },
  app: {
    description: "App description"
  }
};

export default en;
